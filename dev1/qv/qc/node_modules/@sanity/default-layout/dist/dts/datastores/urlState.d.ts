import { Observable } from 'rxjs';
interface ResolvedStateEvent {
    type: 'resolved';
    intent: {
        name: string;
        params: {
            [key: string]: string;
        };
    };
    isNotFound: boolean;
    state: Record<string, unknown>;
}
interface ErrorStateEvent {
    type: 'error';
    error: Error;
}
declare type StateEvent = ResolvedStateEvent | ErrorStateEvent;
export declare function navigate(newUrl: string, options: {
    replace: boolean;
}): void;
export declare const state: Observable<StateEvent>;
export declare function isResolvedEvent(event: StateEvent): event is ResolvedStateEvent;
export {};
//# sourceMappingURL=urlState.d.ts.map